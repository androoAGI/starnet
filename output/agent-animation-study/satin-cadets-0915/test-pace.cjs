const fs=require('fs'),vm=require('vm'),assert=require('node:assert/strict');const w=fs.readFileSync('frontend/agent-demo/review-world.js','utf8');let time=1000;const c={performance:{now:()=>time},Math};vm.createContext(c);vm.runInContext(w.slice(w.indexOf('  const DIR_A ='),w.indexOf('  /* ================= furniture')),c);
const rows=[];for(const item of JSON.parse(fs.readFileSync('frontend/agent-demo/catalog.json')).skins){const b={skin:item.renderSet,dir:'east',faceDir:'east',faceA:0,odo:0,spd:0};let distance=0;for(let i=0;i<120;i++){time+=1000/60;distance+=c.stepGait(b,100,0,100,28,false,1000/60);}assert(Math.abs(b.spd-28)<.001,'Skin slowed by art size '+item.id);assert(distance>52&&distance<=56,'Wrong travel distance '+item.id);rows.push({id:item.id,speed:b.spd,distance});}
console.log({skins:rows.length,cruiseSpeed:28,twoSecondDistance:rows[0].distance});fs.writeFileSync('output/agent-animation-study/satin-cadets-0915/pace-validation.json',JSON.stringify(rows,null,2)+'\n');
