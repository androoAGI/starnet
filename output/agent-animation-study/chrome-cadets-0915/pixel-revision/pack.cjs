const fs=require('fs'),sharp=require('sharp'),base='output/agent-animation-study/chrome-cadets-0915/pixel-revision';
(async()=>{for(const id of ['android','blank_green']){const src=base+'/'+id+'-front-source.png',{data,info}=await sharp(src).ensureAlpha().raw().toBuffer({resolveWithObject:true});let l=info.width,t=info.height,r=-1,b=-1;for(let y=0;y<info.height;y++)for(let x=0;x<info.width;x++)if(data[(y*info.width+x)*4+3]>100){l=Math.min(l,x);t=Math.min(t,y);r=Math.max(r,x);b=Math.max(b,y);}const crop={left:l,top:t,width:r-l+1,height:b-t+1};for(const h of [19,76])await sharp(src).extract(crop).resize({height:h,kernel:'nearest'}).png().toFile(base+'/'+id+'-'+h+'px.png');console.log({id,sourceRatio:crop.width/crop.height,height:19});}})();
