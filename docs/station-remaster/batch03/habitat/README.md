# Painted habitat set

Eight new catalog IDs, individually generated from the corrected illustrated direction: three woven rugs, floor lantern, wood-step ladder, rubber cable run, hazard mat and ceramic-collared vent. The native-scale sheet was visually inspected at 4x world scale. These are source candidates, not proof of running-station integration or user acceptance.

Sources and exact prompts are retained. Hazard pad initially contained a painted checkerboard; ImageGen replaced its background with magenta, retaining both generated sources. Alpha export uses declared magenta key components and manually verified ladder opening seeds, with no RGB changes relative to each final source. Export checks include source/output hashes and crop. No upright images were rotated or stretched.

`anchors.json` records uniform bottom-aligned fits and approximate manually inspected motion/light content guides in source pixels, source-normalized, export-normalized and world coordinates. Rug, cable run and hazard pad are flat floor-pass objects whose quarter turns are valid. The lantern, ladder and vent have one south view. Decoration fits within native bounds; the rug and cable run use less height than the previous native silhouette, preserving their new proportions.

Keep the existing floor-pass, collision and light contracts. The lantern has a steady warm light; the vent's mouth is empty for cosmetic steam and clock-driven light. Cable pulses are cosmetic, never evidence of provider work. No live status is baked into these sources. Validate projected light, steam and floor layering in the running station.
