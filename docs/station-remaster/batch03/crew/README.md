# Bench illustrated-style calibration

Single bounded calibration sample in response to the user's rejection of overly photoreal props. The batch02 bench supplied silhouette/camera/material identity only; the owner workstation supplied rendering-style guidance. One built-in imagegen style-transfer call authored the sample. No other props were restyled.

Olive cushions now use broad angular painted value planes and seam lines, without weave or fuzz. Wood uses warm painted planes and a few broad marks instead of photographic grain. Dark edge separations and selective highlights preserve legible grown-up game construction. Material diversity, horizontal camera, rear-facing occlusion rail and three feet remain.

The complete original is `industrial_bench-source.png`, exact prompt is `industrial_bench.prompt.txt`, and export receipt is `industrial_bench.export.json`. User-authorized deterministic background extraction preserved all source RGB, producing 2054x516 RGBA with 209167 transparent / 850697 opaque / 0 partial pixels. Native-resolution source and the dark-background/native-envelope preview were visually inspected. Exporter syntax and metadata checks passed.

The prior bench shape is preserved: its alpha aspect ratio is approximately4:1, fitting uniformly to width36 and about9px tall inside the existing36x12 envelope, bottom-aligned at y12. The native3x1 footprint, couch-planner sitting claims and rear-rail occlusion remain integration contracts. This is a calibration candidate, not user acceptance or live seat verification. Runtime and all other props are unchanged.

Output: `frontend/assets/industrial/batch03/crew/industrial_bench.png`.
Preview: `review-industrial_bench.png`.
