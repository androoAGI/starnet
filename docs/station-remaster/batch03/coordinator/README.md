# Painted coffee and archive fixtures

Nine complete replacements use built-in imagegen style transfer. Their old object art supplies identity and geometry; the accepted workstation and new painted bookshelf supply rendering references. Exact prompts and unmodified sources are adjacent. Final PNGs and `export-checks.json` are under `frontend/assets/industrial/batch03/coordinator/`.

The authorized deterministic exporter changes alpha and crop only. Whiteboard and chartwall returned opaque black exterior backgrounds; those two use explicitly recorded connected near-black extraction. The other sources use the existing connected light-neutral rule or preserve native alpha. Every retained RGB byte remains unchanged. Browser comparison inspection confirmed complete frames, feet, blank surfaces and no rectangular backgrounds.

The source canvas changed during repainting. Do not reuse batch02 source pixel coordinates blindly. The following are approximate clean regions in the NEW FULL source image, before subtracting the recorded crop:

- Mission board: left x190–550, middle x650–983, right x1080–1400; each y325–555. Keep actual cards inside the three empty panels, clear of physical hooks and frame. Source1610×977.
- Trophy case: x220–890, with clear upper y325–463, middle y610–720 and lower y860–970 shelf regions. Preserve glass/door occlusion over real earned objects. Source1150×1367.
- Microfiche: blank screen approximately x403–988/y324–656. Source1394×1128. Display content must come from runtime truth.
- Inbox: upper x211–1185/y101–275, middle x217–1225/y450–515, lower x225–1245/y684–733. Source1536×1024. All trays remain empty.

Whiteboard, calendar cells and archive drawer labels remain blank. Chartwall diagrams are decorative sketches. Coffee keeps its cup/nozzle geometry; steam/use animation and optional table mounting remain runtime integration work.

These coordinates guide integration and require live alignment checks. All 44 earlier-batch repaints are candidates rather than accepted live props. New catalog sections are tracked separately.
