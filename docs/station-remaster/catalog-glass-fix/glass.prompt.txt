Generate ONE isolated 2D game sprite: the EAST-facing long narrow GLASS TABLE for StarNet. The FIRST reference is a low-resolution native GEOMETRY GUIDE only: match its overall opaque silhouette proportions and tabletop-to-leg division, but do not copy its pixel style, colours or objects underneath. The SECOND reference sheet supplies the accepted glass/black-metal MATERIAL and painted rendering style, especially its second row. Do not generate a sheet or other tables.

CRITICAL: the OBJECT'S opaque bounding box, not the canvas, must have width:height exactly 1:3, within a few percent. Example measurable geometry: on a SQUARE1024x1024 transparent canvas, put a body approximately280 pixels wide by840 pixels tall, centred. Leave broad transparent margins left/right. Do NOT make an extremely thin needle or a4.5:1 table. Width280, totalheight840 means table is THREE times as tall as wide.

Top-down high overhead view, long table axis straight UP/DOWN, all short ends HORIZONTAL, parallel long rails vertical. NO sideways yaw, NO isometric diagonal. This is a horizontal piece of furniture receding away on the floor, NOT a standing mirror or glass door.

Opaque geometry budget: glass tabletop/frame occupies the TOP80% of total height (about672px of840), and the two short near legs plus air gap occupy the BOTTOM20% (about168px). Thus the glass slab itself is only about2.4 times as long as it is wide. Strong dark frame, blue-grey glass with2 broad diagonal painted reflection bands, four small corner fasteners, slim black/charcoal rails, two plainly visible short near legs and one crossbar with transparent air gaps. No shelf clutter, no objects on the top. Render enough thickness to read as a table, but no tall apron. Keep opaque width nearly constant at280 throughout tabletop; feet do not splay wider.

Mature coarse painted raster matching the glass table in reference2, restrained highlights, simple clean planes. No photoreal shader, microtexture or vector icon. Genuine transparent RGBA around and BETWEEN legs, no floor, no shadow blob, no text. The width/height geometry and80/20 table/legs split are more important than adding detail.
