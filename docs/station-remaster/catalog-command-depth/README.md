# Rejected depth revision

This built-in edit was not selected. The output adds background haze and still misses the intended deeper hologram tabletop. No exported sprite or runtime manifest points at this source. The existing selected command candidates remain unchanged.
