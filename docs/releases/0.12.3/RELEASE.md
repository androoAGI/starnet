# Final sprite-animation release 0.12.3

The owner authorized inclusion of the final sprite-animation merge, a fresh rebuild and verification, and publication when all required checks pass (September 17, 2026). This supersedes the earlier instruction to stop before Publish. Customer messages and account actions remain outside scope; the late-email intake remains deferred.

Sprite commit `775fc99c46312cb0bac074ff33d11669e41d538e`, merged through `e681e59b4`, replaces the walk cycles of 33 redesigned skins with eight frames in each of eight directions and repairs their manifest pointers. Capybara retains its earlier frames; the four retained-original skins are unchanged. The animation lane's [known visual limitations](../../../output/agent-animation-study/walk-cycle-fix/README.md) remain explicit. All previously accepted overhaul and customer-fix commits remain included.

The complete 0.12.2 candidate passed acceptance but was not published. Its immutable tag and verified artifacts are preserved as superseded by this final artwork addition. 0.12.0 and 0.12.1 remain rejected historical candidates.

Before publication: fresh source fast/HTTP and Guardian gates, Beginner, installed smoke/readiness, signed three-platform builds, Apple notarization and Intel installed acceptance, Windows upgrade continuity, exact official installer state/catalog/tray/graphics/Refit checks, complete packaged walk-frame and renderer verification, T0 clean installation and all G1 lifecycle cases. The existing owner duration waiver remains scoped to this overhaul; fresh 20-minute standard and 10-minute scale scripted soaks are required, without claiming 12/48-hour observation.

After publication: verify the live three-platform updater manifest, prove an older public Windows client installs and restarts through UPDATE CENTER, and verify the source-repository release mirror. Publication is conditional on the prepublication evidence, not the passage of time. The six previously documented P2 support investigations and deferred late emails are not counted as resolved.

Candidate-bound machine receipts and the final acceptance/publication report are retained under the release lane's `.dogfood/release-recovery/` and copied to `Desktop/gen/release/` for handoff.
