# Corrected overhaul candidate 0.12.1

**REJECTED — DO NOT PUBLISH.** Official installed acceptance found the commander's saved model cleared during catalog unavailability. Controlled installed reproduction confirms the HTTP 200 error-envelope defect, tracked as `ebe2861a`. The original model was manually restored. The successful private checks below remain historical and do not clear this later failure. The signed draft is marked superseded; a new patch candidate is required.

The complete overhaul intended for 0.12.0 is retained. The final hosted G1 gate caught a native close-to-tray failure before publication: finishing a page load could reopen a window the user closed. The 0.12.0 draft is labeled superseded/do not publish. Public latest remains 0.11.2. The release runbook requires a new patch after a built tag fails; no tag is moved and no published asset is replaced.

Frozen application candidate: `9bf98816021fc0d74d4e35c47ec9a1a06b9f2948`. Native repair: `9c3b7819c`. The page-load callback now reveals the initial completed document once, cancels that permission on close, and leaves explicit tray Open/second-launch behavior intact. Start-minimized stays hidden. Four Rust behavior tests and the lifecycle contract test pass; the full pre-merge fast gate passed 814/814.

The private native installer was built from that clean source and installed after a fresh verified backup. All 26 agents, 76 conversations, histories, deliverables, saved agent settings, provider preferences and station layout were preserved. The original close-to-tray matrix now passes, including hidden residency and same-process reveal. A real hidden-window reload reopens the rejected 0.12.0 app but stays hidden in 0.12.1. The private installer hash is `df29aee4897397d8912c7ded5136fc30d85dd7e9b1cd10718f058c3865e4c4ae`; it is updater-signed local verification, not official Azure signing.

The application candidate stays frozen while later commits record acceptance results. Those documentation/register updates do not change the application or version pins. Readiness must explicitly target the frozen candidate and retain each actual binary hash. No receipt is relabeled as a different build.

The support scope remains [the final September 16 intake](../0.12.0/FINAL_INTAKE.md): 83 customer/owner reports, 76 source-fixed, one permission-policy disposition and six open P2 investigations. The new tray defect is an audit finding, independently recorded as `08eece34`. Source inclusion is not customer-confirmed recovery. The owner's duration waiver remains the acceptance boundary for this corrected build of the same update; signing, installed behavior, data preservation and packaged tests are still required.

Required before handoff: fresh Guardian and Beginner acceptance for the frozen candidate; signed three-platform release train; Apple notarization/Gatekeeper and Intel installed acceptance; published Windows upgrade continuity; exact official Windows install, graphics/Refit/data checks; T0 clean-machine install and all G1 lifecycle cases; verified unpublished draft and update manifest. The owner requested a stop immediately before publication. No website deployment, support reply, refund or publication is authorized by this document.
